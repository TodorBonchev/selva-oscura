PORT=8080
# CONTENT_ROOT=/app/content
# Local Postgres from `npm run db` (no SSL). Do not commit a filled .env.
# DATABASE_URL=postgresql://selva:selva_local@127.0.0.1:5433/selva
# Production Neon (SSL). Omit both to run in-memory.
# DATABASE_URL=postgresql://USER:PASS@HOST/DB?sslmode=require
